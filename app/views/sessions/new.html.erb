<div class="min-h-screen flex items-center justify-center bg-gray-100 px-4 relative">
  
  <%# ปุ่มย้อนกลับชิดมุมซ้ายบน %>
  <a href="/" class="absolute top-6 left-6 flex items-center gap-2 text-gray-500 hover:text-gray-800 font-bold transition">
    <span class="text-xl">←</span> กลับหน้าหลัก
  </a>

  <div class="max-w-md w-full bg-white rounded-2xl shadow-2xl overflow-hidden border border-gray-200">
    
    <div class="bg-gray-800 p-8 text-center">
      <div class="text-4xl mb-2">🎨</div>
      <h2 class="text-3xl font-bold text-white tracking-tight">เข้าสู่ระบบ</h2>
      <p class="text-gray-400 mt-2">เพื่อบันทึก Palette สีของคุณ</p>
    </div>

    <div class="p-8">
      <% if flash[:alert] %>
        <div class="mb-4 bg-red-50 border-l-4 border-red-500 text-red-700 p-4 rounded" role="alert">
          <p class="font-bold">ผิดพลาด</p>
          <p><%= flash[:alert] %></p>
        </div>
      <% end %>

      <%= form_with url: login_path, local: true do |form| %>
        <div class="mb-5">
          <%= form.label :email, "อีเมล", class: "block text-xs font-bold text-gray-500 uppercase mb-2" %>
          <%= form.email_field :email, placeholder: "you@example.com", required: true,
              class: "w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 focus:ring-2 focus:ring-blue-500 outline-none transition" %>
        </div>

        <div class="mb-5">
          <%= form.label :password, "รหัสผ่าน", class: "block text-xs font-bold text-gray-500 uppercase mb-2" %>
          <%= form.password_field :password, placeholder: "••••••••", required: true,
              class: "w-full px-4 py-3 rounded-xl bg-gray-50 border border-gray-200 focus:ring-2 focus:ring-blue-500 outline-none transition" %>
        </div>

        <div class="flex items-center justify-between mb-8">
          <label class="flex items-center text-sm text-gray-600 cursor-pointer">
            <input type="checkbox" class="mr-2 h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500">
            จำฉันไว้
          </label>
          <a href="#" class="text-sm text-blue-600 font-bold hover:underline">ลืมรหัสผ่าน?</a>
        </div>

        <%= form.submit "เข้าสู่ระบบ", 
            class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-4 rounded-xl transition duration-300 shadow-lg cursor-pointer" %>
      <% end %>

      <div class="mt-8 pt-6 border-t border-gray-100 text-center">
        <p class="text-gray-500">
          ยังไม่มีบัญชี? 
          <a href="/signup" class="text-blue-600 font-bold hover:underline">สมัครสมาชิกฟรี</a>
        </p>
      </div>
    </div>
  </div>
</div>